{
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/((?!sitemap\\.xml|favicon\\.svg|icons\\.svg|robots\\.txt).+)",
      "destination": "/index.html"
    }
  ]
}
